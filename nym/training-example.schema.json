{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/byteowlz/nym/schemas/training-example.schema.json",
  "title": "PII Training Example Schema",
  "description": "Schema for training examples with input_text and expected LLM output.",
  "type": "object",
  "required": ["input_text", "expected_json"],
  "properties": {
    "input_text": {
      "type": "string",
      "description": "The input text to analyze for PII entities."
    },
    "expected_json": {
      "$ref": "https://github.com/byteowlz/nym/schemas/pii-detection-output.schema.json",
      "description": "The expected LLM output following the PII detection output schema."
    }
  },
  "additionalProperties": false
}
